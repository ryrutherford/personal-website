import React from "react";
import SVGBox from "./SVGBox";

const Resume = () => {
    return(
        <section>
            <SVGBox steps={["Resume", 1500]} svgStyle="endless-clouds"/>
        </section>
    );
}

export default Resume;